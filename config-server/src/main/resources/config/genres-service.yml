spring:
  datasource:
    url: jdbc:sqlite:src/main/resources/db/database.sqlite
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: com.javatar.genresservice.config.SQLiteDialect
    hibernate:
      ddl-auto: none
    show-sql: true
